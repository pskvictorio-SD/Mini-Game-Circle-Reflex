{
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "mysql2": "^3.16.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}